import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'lista_tareas_react_simple',
  webDir: 'dist'
};

export default config;
